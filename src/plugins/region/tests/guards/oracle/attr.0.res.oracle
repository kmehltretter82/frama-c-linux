[kernel] Parsing attr.i (no preprocessing)
[region] annotating function allocated_arr
[region] annotating function allocated_nullable_arr
[region] annotating function allocated_nullable_ptr
[region] annotating function allocated_ptr
[region] annotating function default_arr
[region] annotating function default_ptr
[region] annotating function garbage_arr
[region] annotating function garbage_ptr
[region] annotating function nullable_arr
[region] annotating function nullable_ptr
/* Generated by Frama-C */
/*@ \region::region p[0..0]; */
int default_ptr(int *p)
{
  int __retres;
  __retres = *p;
  return __retres;
}

/*@ \region::region p[0..n - 1]; */
int default_arr(int *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p + k); */
  /*@ check region: path: \validregion(p + k, 4); */
  __retres = *(p + k);
  return __retres;
}

/*@ \region::region p[0..0], \nullable; */
int nullable_ptr(int *p)
{
  int __retres;
  /*@ check region: mem_access: p ≢ \null; */
  __retres = *p;
  return __retres;
}

/*@ \region::region p[0..n - 1], \nullable; */
int nullable_arr(int *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p + k); */
  /*@ check region: path: \validregion(p + k, 4); */
  __retres = *(p + k);
  return __retres;
}

/*@ \region::region p[0..0], \allocated; */
int allocated_ptr(int *p)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p); */
  /*@ check region: initialized: \initialized(p); */
  __retres = *p;
  /*@ check region: initialized: \initialized(&__retres); */
  return __retres;
}

/*@ \region::region p[0..n - 1], \allocated; */
int allocated_arr(int *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p + k); */
  /*@ check region: path: \validregion(p + k, 4); */
  /*@ check region: initialized: \initialized(p + k); */
  __retres = *(p + k);
  /*@ check region: initialized: \initialized(&__retres); */
  return __retres;
}

/*@ \region::region p[0..0], \nullable, \allocated; */
int allocated_nullable_ptr(int *p)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p); */
  /*@ check region: initialized: \initialized(p); */
  __retres = *p;
  /*@ check region: initialized: \initialized(&__retres); */
  return __retres;
}

/*@ \region::region p[0..n - 1], \nullable, \allocated; */
int allocated_nullable_arr(int *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p + k); */
  /*@ check region: path: \validregion(p + k, 4); */
  /*@ check region: initialized: \initialized(p + k); */
  __retres = *(p + k);
  /*@ check region: initialized: \initialized(&__retres); */
  return __retres;
}

/*@ \region::region p[0..0], \garbage; */
int garbage_ptr(int *p)
{
  int __retres;
  /*@ check region: initialized: \initialized(p); */
  __retres = *p;
  /*@ check region: initialized: \initialized(&__retres); */
  return __retres;
}

/*@ \region::region p[0..n - 1], \garbage; */
int garbage_arr(int *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p + k); */
  /*@ check region: path: \validregion(p + k, 4); */
  /*@ check region: initialized: \initialized(p + k); */
  __retres = *(p + k);
  /*@ check region: initialized: \initialized(&__retres); */
  return __retres;
}


