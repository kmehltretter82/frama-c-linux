[kernel] Parsing valid.i (no preprocessing)
[region] annotating function default_arr
[region] annotating function default_cast
[region] annotating function default_comp
[region] annotating function default_nocast
[region] annotating function default_pcomp
[region] annotating function default_ptr
/* Generated by Frama-C */
struct S {
   int f[5] ;
};
/*@ \region::region p[0..0]; */
int default_ptr(int *p)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p); */
  /*@ check region: initialized: \initialized(p); */
  __retres = *p;
  return __retres;
}

/*@ \region::region p[0..n - 1]; */
int default_arr(int *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read(p + k); */
  /*@ check region: boundaries: \valid_region(p + k, 4); */
  /*@ check region: initialized: \initialized(p + k); */
  __retres = *(p + k);
  return __retres;
}

/*@ \region::region p; */
int default_comp(struct S p, int k)
{
  int __retres;
  /*@ check region: safe_array: 0 ≤ k < 5; */
  /*@ check region: initialized: \initialized(&p.f[k]); */
  __retres = p.f[k];
  return __retres;
}

/*@ \region::region p[0..0]; */
int default_pcomp(struct S *p, int k)
{
  int __retres;
  /*@ check region: safe_array: 0 ≤ k < 5; */
  /*@ check region: mem_access: \valid_read(&p->f[k]); */
  /*@ check region: initialized: \initialized(&p->f[k]); */
  __retres = p->f[k];
  return __retres;
}

/*@ \region::region p[0..n - 1]; */
int default_cast(char *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read((int *)(p + k)); */
  /*@ check region: boundaries: \valid_region((int *)(p + k), 4); */
  /*@ check region: initialized: \initialized((int *)(p + k)); */
  __retres = *((int *)(p + k));
  return __retres;
}

/*@ \region::region (int *)p[0..n - 1]; */
int default_nocast(char *p, int k, int n)
{
  int __retres;
  /*@ check region: mem_access: \valid_read((int *)p + k); */
  /*@ check region: boundaries: \valid_region((int *)p + k, 4); */
  /*@ check region: initialized: \initialized((int *)p + k); */
  __retres = *((int *)p + k);
  return __retres;
}


