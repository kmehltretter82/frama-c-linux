[kernel] Parsing sepflags.i (no preprocessing)
[region] annotating function job
/* Generated by Frama-C */
/*@ \region::region A: a[0..n], \nullable;
    \region::region B: b[0..n], \nullable;
    assigns \nothing;
 */
void f(int *a, int *b, int n);

/*@ \region::region a[0..n], b[0..n], \nullable; */
void job(int *a, int *b, int n)
{
  /*@ check
        region: f: A: B:
          a ≢ \null ⇒ b ≢ \null ⇒ \separated(a + (0 .. n), b + (0 .. n));
  */
  f(a,b,n);
  return;
}


