[kernel] Parsing sepflags.i (no preprocessing)
[region] annotating function job
/* Generated by Frama-C */
/*@ \region::region A: a[0..n];
    \region::region B: b[0..n], \nullable;
    \region::region R: c[0..n], \allocated;
    \region::region R: d[0..n], \nullable, \allocated;
    assigns \nothing;
 */
void f(int *a, int *b, int *c, int *d, int n);

/*@ \region::region a[0..n], b[0..n], *(c + (..)), *(d + (..)); */
void job(int *a, int *b, int *c, int *d, int n)
{
  /*@ check
        region: f: A: B: b ≢ \null ⇒ \separated(a + (0 .. n), b + (0 .. n));
  */
  /*@ check
        region: f: A: R:
          \valid_read(c + (0 .. n)) ⇒ \separated(a + (0 .. n), c + (0 .. n));
  */
  /*@ check
        region: f: A: R:
          \valid_read(d + (0 .. n)) ⇒ \separated(a + (0 .. n), d + (0 .. n));
  */
  /*@ check
        region: f: B: R:
          b ≢ \null ⇒
          \valid_read(c + (0 .. n)) ⇒ \separated(b + (0 .. n), c + (0 .. n));
  */
  /*@ check
        region: f: B: R:
          b ≢ \null ⇒
          \valid_read(d + (0 .. n)) ⇒ \separated(b + (0 .. n), d + (0 .. n));
  */
  f(a,b,c,d,n);
  return;
}


