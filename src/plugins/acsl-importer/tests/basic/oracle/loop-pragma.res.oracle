[kernel] Parsing loop-pragma.i (no preprocessing)
[acsl-import] Success for loop-pragma.acsl
[acsl-import] Done: 1 file.
/* Generated by Frama-C */
int z;
void main(int a)
{
  int x;
  int y;
  x = 0;
  if (! (x < a)) goto unfolding_2_loop;
  y = 0;
  /*@ loop invariant y ≤ a;
      loop unfold "done";
      loop unfold 1; */
  while (y < x) {
    z ++;
    y ++;
  }
  x ++;
  unfolding_3_loop: ;
  /*@ loop unfold 1;
      loop unfold "done", 1; */
  while (x < a) {
    {
      y = 0;
      /*@ loop invariant y ≤ a;
          loop unfold 1;
          loop unfold "done"; */
      while (y < x) {
        {
          z ++;
        }
        y ++;
      }
    }
    x ++;
  }
  unfolding_2_loop: ;
  return;
}


[kernel] Parsing ocode_0_loop-pragma.i (no preprocessing)
