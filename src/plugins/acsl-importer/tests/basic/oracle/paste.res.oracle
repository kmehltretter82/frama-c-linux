[kernel] Parsing paste.i (no preprocessing)
[acsl-import] Success for paste.acsl
[acsl-import] Done: 1 file.
/* Generated by Frama-C */
int volatile z;
int rd_z_cpt;
extern int rd_z_tab[];

/*@ requires p ≡ &z;
    ensures rd_z_cpt ≡ \old(rd_z_cpt) + 1;
    ensures \result ≡ \old(rd_z_tab[rd_z_cpt]);
    assigns rd_z_cpt;
 */
int rd_z(int volatile *p)
{
  int result = rd_z_tab[rd_z_cpt];
  rd_z_cpt ++;
  return result;
}

/*@ volatile z reads rd_z;
*/
/*@ requires pre: \true;
    requires pre: x > 0;
    requires file1: \true;
    requires file2: \true;
    requires includes_file3: \true;
    requires includes_file1: \true;
    requires includes_file2: \true;
    ensures returns_1: \result ≥ 1;
    ensures returns_1: \result ≤ 1;
 */
int main(int x)
{
  int __retres;
  l:
  /*@ assert at_l: x > 0; */
  {
    int y = x;
    int x_0 = y;
    l1:
    /*@ assert at_l1: x_0 > 0 ∧ x_0 ≡ \at(x,Pre); */
    {
      int x_1 = y;
      if (x_1 > 10) {
        __retres = x_1;
        goto return_label;
      }
      l11: /*@ assert at_l11: x_1 > 0 ∧ \at(x,Pre) ≡ \at(x_0,l1); */
      y = x_1;
    }
    l2:
    /*@ assert at_l2: x_0 > 0 ∧ x_0 ≡ \at(x,Pre); */
    {
      int x_2 = y + z;
      l21:
      /*@ assert at_l21: x_2 > 0 ∧ x_2 ≡ \at(x,Pre); */
      {
        __retres = x_2 + y;
        return_label:
        /*@ assert at_return: x_2 > 0 ∧ \at(&x,Pre) ≢ \at(&x_0,l2); */
        return __retres;
      }
    }
  }
}


[kernel] Parsing ocode_0_paste.i (no preprocessing)
