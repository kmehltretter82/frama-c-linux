[kernel] Parsing macros.i (no preprocessing)
[acsl-import] Success for macros.fc
[acsl-import] Done: 1 file.
/* Generated by Frama-C */
/*@ axiomatic B {
      predicate False= \false;
      
    }
 */
/*@ axiomatic A {
      predicate P(ℤ u, ℤ v, ℤ w, ℤ x) ;
      
    }
 */
/*@
axiomatic ArraySize {
  logic ℤ logic_size= 20;
  
  logic ℤ indirect_logic_size= logic_size;
  
  logic ℤ indirect_macro_size= 10;
  
  type Tint10 = int [10];
  
  predicate Parray(Tint10 tab10, int t20[20], int t10[10]) ;
  
}
 */
/*@ requires p3: P(w, x, x, z);
    requires p2: P(2, x, y, z);
    requires p1: P(1, x, y, z);
    requires file: \true;
 */
extern void f(int w, int x, int y, int z);

/*@ requires p3: P(a, x, x, z);
    requires file: \true; */
void g(int a, int b, int x, int z)
{
  f(a,b,x,z);
  return;
}


[kernel] Parsing ocode_0_macros.i (no preprocessing)
