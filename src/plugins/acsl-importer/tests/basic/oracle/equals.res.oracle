[kernel] Parsing equals.i (no preprocessing)
[acsl-import] Success for equals.acsl
[acsl-import] Done: 1 file.
/* Generated by Frama-C */
/*@
predicate is_equal{A, B}(unsigned int *a, unsigned int n, unsigned int *b) =
  ∀ ℤ i; 0 ≤ i < n ⇒ \at(*(a + i),A) ≡ \at(*(b + i),B);
 */
/*@ requires n > 0 ⇒ \valid(a + (0 .. n - 1)) ∧ \valid(b + (0 .. n - 1));
    ensures \result ≢ 0 ⇔ is_equal{Here, Here}(\old(a), \old(n), \old(b));
 */
int equals(unsigned int const *a, unsigned int n, unsigned int const *b)
{
  int __retres;
  unsigned int i;
  i = (unsigned int)0;
  /*@ loop assigns i;
      loop variant n - i; */
  while (i < n) {
    /*@ invariant 0 ≤ i; */
    /*@ invariant is_equal{Here, Here}(a, i, b); */
    {
      if (*(a + i) != *(b + i)) {
        __retres = 0;
        goto return_label;
      }
    }
    i ++;
  }
  __retres = 1;
  return_label: return __retres;
}


[kernel] Parsing ocode_0_equals.i (no preprocessing)
