# frama-c -wp [...]
[kernel] Parsing lemma_dependency.i (no preprocessing)
[wp] Running WP plugin...
------------------------------------------------------------
  Global
------------------------------------------------------------

Goal Lemma 'depends':
Assume Lemmas: 'dependency'
Assume {
  Have: (c land (a lxor b)) != 0.
  Have: is_uint8(a).
  Have: is_uint8(b).
  Have: is_uint8(c).
}
Prove: (b land c) != (a land c).

------------------------------------------------------------
