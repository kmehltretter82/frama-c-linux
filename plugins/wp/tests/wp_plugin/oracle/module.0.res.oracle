# frama-c -wp [...]
[kernel] Parsing module.i (no preprocessing)
[wp] Running WP plugin...
------------------------------------------------------------
  Global
------------------------------------------------------------

Goal Lemma 'AbsOp':
Let x_1 = abs(n). Prove: L_fooJazzopN(x, x_1) = L_fooBaropN(x, x_1).

------------------------------------------------------------
