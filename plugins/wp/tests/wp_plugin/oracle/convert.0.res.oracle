# frama-c -wp [...]
[kernel] Parsing convert.i (no preprocessing)
[wp] Running WP plugin...
------------------------------------------------------------
  Global
------------------------------------------------------------

Goal Lemma 'ceil':
Assume Lemmas: 'floor'
Let x_1 = ceil(x). Prove: (x <= from_int(x_1)) /\ (from_int(x_1 - 1) < x).

------------------------------------------------------------

Goal Lemma 'floor':
Let x_1 = floor(x). Prove: (from_int(x_1) <= x) /\ (x < from_int(1 + x_1)).

------------------------------------------------------------
