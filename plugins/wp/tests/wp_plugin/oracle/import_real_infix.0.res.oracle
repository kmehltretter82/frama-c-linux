# frama-c -wp [...]
[kernel] Parsing import_real_infix.i (no preprocessing)
[wp] Running WP plugin...
------------------------------------------------------------
  Global
------------------------------------------------------------

Goal Lemma 'infix':
Prove: (<.)((+.)(.0, 1.0), 2.0).

------------------------------------------------------------
