[kernel] Parsing flags.c (with preprocessing)
[kernel:annot:missing-spec] flags.c:29: Warning: 
  Neither code nor explicit assigns for function spec_nullread, generating
  default clauses. See -generated-spec-* options for more info.
[region] annotating function context_allocated
[region] annotating function context_allocread
[region] annotating function context_default
[region] annotating function context_dummy
[region] annotating function context_garbage
[region] annotating function context_nullable
[region] annotating function context_nullalloc
[region] annotating function context_nullgarb
[region] annotating function context_nullread
/* Generated by Frama-C */
/*@ \region::region a[0..0]; */
void spec_default(int *a);

/*@ \region::region a[0..0], \nullable; */
void spec_null(int *a);

/*@ \region::region a[0..0], \allocated; */
void spec_allocated(int *a);

/*@ \region::region a[0..0], \nullable, \allocated; */
void spec_nullalloc(int *a);

/*@ \region::region a[0..0], \validread; */
void spec_validread(int *a);

/*@ \region::region a[0..0], \nullable, \validread;
    assigns *a;
    assigns *a \from *a;
 */
void spec_nullread(int *a);

/*@ \region::region a[0..0], \garbage; */
void spec_garbage(int *a);

/*@ \region::region a[0..0], \nullable, \garbage; */
void spec_nullgarb(int *a);

/*@ \region::region a[0..0], \nullable, \allocated, \garbage, \validread; */
void spec_dummy(int *a);

/*@ \region::region a_default[0..0]; */
void context_default(int *a_default)
{
  spec_nullread(a_default);
  return;
}

/*@ \region::region a_nullable[0..0], \nullable; */
void context_nullable(int *a_nullable)
{
  spec_nullread(a_nullable);
  return;
}

/*@ \region::region a_allocated[0..0], \allocated; */
void context_allocated(int *a_allocated)
{
  /*@ check
        region: spec_nullread: a_allocated ≢ \null ⇒ \valid_read(a_allocated);
  */
  spec_nullread(a_allocated);
  return;
}

/*@ \region::region a_allocread[0..0], \allocated, \validread; */
void context_allocread(int *a_allocread)
{
  /*@ check
        region: spec_nullread: a_allocread ≢ \null ⇒ \valid_read(a_allocread);
  */
  spec_nullread(a_allocread);
  return;
}

/*@ \region::region a_nullread[0..0], \nullable, \validread; */
void context_nullread(int *a_nullread)
{
  spec_nullread(a_nullread);
  return;
}

/*@ \region::region a_nullalloc[0..0], \nullable, \allocated; */
void context_nullalloc(int *a_nullalloc)
{
  /*@ check
        region: spec_nullread: a_nullalloc ≢ \null ⇒ \valid_read(a_nullalloc);
  */
  spec_nullread(a_nullalloc);
  return;
}

/*@ \region::region a_garbage[0..0], \garbage; */
void context_garbage(int *a_garbage)
{
  /*@ check region: spec_nullread: \initialized(a_garbage); */
  spec_nullread(a_garbage);
  return;
}

/*@ \region::region a_nullgarb[0..0], \nullable, \garbage; */
void context_nullgarb(int *a_nullgarb)
{
  /*@ check
        region: spec_nullread: a_nullgarb ≢ \null ⇒ \initialized(a_nullgarb);
  */
  spec_nullread(a_nullgarb);
  return;
}

/*@ \region::region a_dummy[0..0], \nullable, \allocated, \garbage,
                      \validread;
 */
void context_dummy(int *a_dummy)
{
  /*@ check
        region: spec_nullread:
          a_dummy ≢ \null ⇒ \valid_read(a_dummy) ∧ \initialized(a_dummy);
  */
  spec_nullread(a_dummy);
  return;
}


