[kernel] Parsing globals.i (no preprocessing)
[region] annotating function f
[region] annotating function job
/* Generated by Frama-C */
int K;
/*@ \region::region a[0..0], b[0..0];
    assigns *a, *b, K; */
void f(int *a, int *b)
{
  K ++;
  /*@ check region: \valid_region(a, sizeof(int)); */
  *a = K;
  K ++;
  /*@ check region: \valid_region(b, sizeof(int)); */
  *b = K;
  return;
}

void job(int *p, int *q)
{
  /*@ check region: f: \separated(p, &K); */
  /*@ check region: f: \separated(q, &K); */
  f(p,q);
  return;
}


