[kernel] Parsing separated.i (no preprocessing)
[region] annotating function job1_mix
[region] annotating function job2_mix
[region] annotating function job2_ok
[region] annotating function job3_mix
[region] annotating function job3_ok
[region] annotating function job4_mix
[region] annotating function job4_ok
/* Generated by Frama-C */
/*@ \region::region A: a1[0..n], a2[0..n];
    \region::region B: b1[0..n], b2[0..n];
    assigns \nothing;
 */
void f(int *a1, int *a2, int *b1, int *b2, int n);

/*@ \region::region *(d + (..)); */
void job1_mix(int *d, int a1, int a2, int b1, int b2, int n)
{
  /*@ check
        region: f: A: B: \separated((d + a1) + (0 .. n), (d + b1) + (0 .. n));
  */
  /*@ check
        region: f: A: B: \separated((d + a1) + (0 .. n), (d + b2) + (0 .. n));
  */
  /*@ check
        region: f: A: B: \separated((d + a2) + (0 .. n), (d + b1) + (0 .. n));
  */
  /*@ check
        region: f: A: B: \separated((d + a2) + (0 .. n), (d + b2) + (0 .. n));
  */
  f(d + a1,d + a2,d + b1,d + b2,n);
  return;
}

/*@ \region::region P: *(a + (..)), Q: *(b + (..)); */
void job2_ok(int *a, int p1, int p2, int *b, int q1, int q2, int n)
{
  f(a + p1,a + p2,b + q1,b + q2,n);
  return;
}

/*@ \region::region P: *(a + (..)), Q: *(b + (..)); */
void job2_mix(int *a, int p1, int p2, int *b, int q1, int q2, int n)
{
  /*@ check
        region: f: A: B: \separated((a + p1) + (0 .. n), (a + p2) + (0 .. n));
  */
  /*@ check
        region: f: A: B: \separated((b + q1) + (0 .. n), (b + q2) + (0 .. n));
  */
  f(a + p1,b + q1,a + p2,b + q2,n);
  return;
}

/*@ \region::region P: *(a + (..)), Q: *(b + (..)), R: *(c + (..)); */
void job3_ok(int *a, int *b, int *c, int p, int q, int n)
{
  f(a,b,c + p,c + q,n);
  return;
}

/*@ \region::region P: *(a + (..)), Q: *(b + (..)), R: *(c + (..)); */
void job3_mix(int *a, int *b, int *c, int p, int q, int n)
{
  /*@ check
        region: f: A: B: \separated((c + p) + (0 .. n), (c + q) + (0 .. n));
  */
  f(a,c + p,b,c + q,n);
  return;
}

/*@ \region::region P: *(a + (..)),
                    Q: *(b + (..)),
                    R: *(c + (..)),
                    S: *(d + (..));
 */
void job4_ok(int *a, int *b, int *c, int *d, int n)
{
  f(a,b,c,d,n);
  return;
}

/*@ \region::region P: *(a + (..)), *(c + (..)), Q: *(b + (..)), *(d + (..));
 */
void job4_mix(int *a, int *b, int *c, int *d, int n)
{
  /*@ check region: f: A: B: \separated(a + (0 .. n), c + (0 .. n)); */
  /*@ check region: f: A: B: \separated(b + (0 .. n), d + (0 .. n)); */
  f(a,b,c,d,n);
  return;
}


