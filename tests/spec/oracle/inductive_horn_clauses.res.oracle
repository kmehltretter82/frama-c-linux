[kernel] Parsing inductive_horn_clauses.i (no preprocessing)
/* Generated by Frama-C */
/*@
inductive incorrect_conclusion(ℤ x) {
  case ko: ∀ ℤ x; incorrect_conclusion(x) ⇒ x ≢ x + 1;
  }
 */
/*@
inductive negative_occurrence(ℤ x) {
  case ko: ∀ ℤ x; ¬negative_occurrence(x) ⇒ negative_occurrence(x);
  }
 */
/*@
inductive negative_through_let(ℤ x) {
  case ko: ∀ ℤ x;
           \let p = \lambda ℤ x, ℤ y; negative_through_let(x + y);
           ¬p(x, 2) ⇒ p(x, 2);
  }
 */
/*@
inductive ok_let(ℤ x) {
  case ok: ∀ ℤ x, ℤ y;
           \let p = \lambda ℤ x, ℤ y; ok_let(x + y); p(x, 0) ⇒ p(x, 1);
  case base: ok_let(0);
  }
 */

