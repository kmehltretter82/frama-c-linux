/* run.config*
   EXIT: 1
   STDOPT:
*/

void main(void) {
	int t[2];
	/* The spaces and tabulations in the next line are not guaranteed to be
	   kept by the preprocessor. This may lead to wrong column counting. */
	int 	    x = t[xy];
}
