/*run.config
  OPT: -autocomplete=
  COMMENT: the following test ensures kernel options are printed;
  COMMENT: but to avoid instable oracles, we filter almost all output
FILTER: sed "/-.*$/d"
  OPT: -autocomplete=kernel
*/
